<%= @guest.name %>
<br>
Rooms Stayed In:
<br>
<% @rooms.each do |room| %>
<%= room.hotel.name %> - <%= room.suite %>
<br>
Nightly Rate: <%= number_to_currency(room.rate) %>
<br>
<% end %>

Add Room:
<%= form_with url: "/guests/#{@guest.id}/guest_rooms", method: :post, local: true do |f| %>
  <%= f.label "Room ID:" %>
  <%= f.number_field :room_id %>
  <%= f.submit "Submit" %>
<% end %>