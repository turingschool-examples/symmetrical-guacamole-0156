<h1>Rooms</h1>
<% @rooms.each do |room| %>
  <h4><%= room.suite %></h4>
  <%= "Rate: #{room.rate}" %><br>
  <%= "hotel: #{room.hotel.name}" %><br>
  <%= "Number of total guests: #{room.guests.count}" %>
<% end %>